name: Semgrep Scanner
on:
  pull_request:
    types:
      - synchronize
      - opened

permissions: {}

jobs:
  scanner:
    permissions:
      contents: read
    uses: ./.github/workflows/action-run-semgrep.yaml
